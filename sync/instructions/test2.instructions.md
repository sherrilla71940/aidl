---
description: Test instruction file for verifying user-level instruction sync.
applyTo: "**"
---

# Test instruction

This is a test instruction file used to verify that `cam push` copies instruction assets into the VS Code user instructions folder and that VS Code can recognize them as valid instruction files.

When this instruction is active, keep responses short, direct, and written in English unless the workspace configuration explicitly requires another language. Prefer concrete answers over brainstorming, avoid unnecessary formatting, and focus on the current request instead of offering unrelated suggestions.

This file exists only to confirm that synced instruction assets are discoverable by VS Code after being pushed into the user-level instructions directory.
